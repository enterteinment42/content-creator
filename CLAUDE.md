# Content Creator — брифинг

> ⚠️ ВАЖНО: этот файл — единственный источник контекста. НЕ читать и НЕ использовать `CLAUDE.md` из родительских директорий (`poigraem/CLAUDE.md` и выше). Задачи основного магазина к этому проекту не относятся.

Отдельное веб-приложение в проекте «Поиграем». Инструмент для создания контента: коллажи обложек, сторис и TG-посты. Работает параллельно с основным магазином — всё что было в админке из этой области переедет сюда.

## Зачем отдельное приложение

В `index.html` основного магазина накопилось >6000 строк. Коллаж, сторис и TG-пост используются только Денисом (не клиентами), но грузятся всем. Вынос в отдельный файл:
- облегчает клиентскую загрузку основного магазина
- даёт чистый отдельный инструмент без лишнего UI
- не ломает основной магазин в процессе переработки

## Стратегия

1. **Этап 1 (сейчас):** создать рабочее приложение параллельно, не трогая основной магазин
2. **Этап 2 (после проверки):** вырезать соответствующие блоки из `index.html` + убрать `collage.js` и `story.js` из основного

## Формат

Единый HTML-файл (как `avito-helper.html`) — открывается через GitHub Pages.
⚠️ Проект вынесен в ОТДЕЛЬНЫЙ репозиторий `github.com/enterteinment42/content-creator` (коммит `54af20a`), файл `content-creator.html` теперь в КОРНЕ репо (не `Standalone/content-creator/…`). Актуальный URL деплоя зависит от настроек Pages нового репо (ожидаемо `https://enterteinment42.github.io/content-creator/content-creator.html` — проверить). Старый путь `…/poigraem/Standalone/content-creator/content-creator.html` устарел.

## Данные

Источник игр — публичный снапшот магазина:
```
GET https://api.poigraem.shop/db/snapshot
→ { games: [...], subs: [...], ... }
```
Авторизация не нужна — снапшот публичный.

Для генерации TG-постов через Claude API:
```
POST https://api.poigraem.shop/api/claude
```
Токен не нужен (прокси открытый). Можно добавить `ADMIN_TOKEN` в localStorage если понадобится.

## Структура игры в снапшоте

```javascript
{
  title: "Spider-Man 2",
  priceRUB: 2500,
  discount: 30,          // процент скидки или null
  icon: "https://...",   // обложка IGDB (t_cover_big)
  featuredImg: "...",    // featured image (если есть)
  description: "...",
  store: "PS",
  hidden: false,
  selected: false        // игнорировать, это UI-флаг
}
```

## Что портируем из основного магазина

| Модуль | Источник | Объём |
|--------|----------|-------|
| Таблица игр с галочками | `js/admin-core.js` (renderAdminTable) | ~150 строк |
| Коллаж | `js/collage.js` | ~500 строк |
| Сторис | `js/story.js` | ~??? строк |
| TG-пост | `js/admin-core.js` (generateTGPost) | ~100 строк |
| Вспомогательные | `js/utils.js`, `js/pricing.js` | ~150 строк |

Всё это копируется/адаптируется в один HTML-файл. Нет import/export — всё глобальное (как в основном магазине).

## UI-концепция

```
┌─────────────────────────────────────────────────┐
│  🎮 Content Creator          [🔄 Обновить]      │
├─────────────────────────────────────────────────┤
│  Поиск: [____________]  Выбрано: 3/5            │
│  ┌──────────────────────────────────────────┐   │
│  │ ☑ Spider-Man 2      2 500 ₽  -30%  🖼    │   │
│  │ ☐ God of War        3 200 ₽       🖼     │   │
│  │ ☑ GTA V             1 800 ₽  -20%  🖼    │   │
│  │ ...                                      │   │
│  └──────────────────────────────────────────┘   │
├─────────────────────────────────────────────────┤
│  [🖼 Коллаж]  [📱 Сторис]  [📢 TG-пост]        │
└─────────────────────────────────────────────────┘
```

- Таблица: название, цена, скидка, миниатюра обложки, чекбокс
- Поиск фильтрует в реальном времени
- Максимум 5 выбранных (для коллажа/сторис)
- Кнопки активируются когда выбрана хотя бы 1 игра
- TG-пост: любое количество игр

## Стиль

Тёмная тема, те же CSS-переменные что в основном магазине:
```css
--bg: #0a0a16;  --card: #0e0e1c;  --border: rgba(255,255,255,.08);
--text: #e8e8f0;  --muted: #666680;  --blue: #0070d1;  --accent: #f0a020;
```
Шрифты: системные (без Google Fonts для скорости).

## Порядок реализации в следующей сессии

1. Скелет HTML: шапка, загрузка снапшота, спиннер
2. Таблица игр с поиском и чекбоксами
3. Порт `collage.js` → встроить в HTML
4. Порт `story.js` → встроить в HTML  
5. Порт TG-пост → встроить в HTML
6. Проверить всё работает
7. После проверки → вырезать из основного `index.html`

## Команда: /session-start

Когда Денис пишет `/session-start` или начинает новый чат с упоминанием проекта:
1. Прочитай раздел `## Текущий фокус`
2. Прочитай 2-3 последние записи в `## Лог сессий`
3. Резюмируй: «В прошлый раз остановились на [...]. Открыто [...]. Следующий шаг был [...].»
4. Спроси: «Продолжаем с этого или новое направление?»

## Команда: /session-end

Когда Денис пишет `/session-end`:
1. Обнови раздел `## Текущий фокус` вверху файла актуальным состоянием
2. Добавь НОВУЮ запись В НАЧАЛО раздела `## Лог сессий` (не редактируй существующие):
   - Дата (текущая)
   - Что сделано — конкретные изменения в коде/фичах
   - Что решили — архитектурные/продуктовые решения
   - Новые баги (если нашли) — с указанием файла
   - Открытые вопросы — что обсуждали но не решили
   - Что осталось — задачи на следующую сессию
3. Если в логе стало больше 5 записей — самые старые (сверх 5) перенеси в `SESSIONS-ARCHIVE.md` (в начало файла, создай его если нет). В CLAUDE.md всегда держим 5 последних сессий.
4. **Перенос закрытого в `AUDITS-ARCHIVE.md`** (создай файл если нет): пройдись по CLAUDE.md и найди аудиты/находки/задачи, которые за эту сессию стали ПОЛНОСТЬЮ закрытыми. Перенеси текст в `AUDITS-ARCHIVE.md` с пометкой «✅ закрыт, сессия N, коммит(ы) …», в CLAUDE.md оставь строку-указатель. ⚠️ Не переноси, если закрытие не подтверждено явно (код готов, но не проверено визуально — это НЕ закрытие по стандарту этого проекта).
5. **Проверка незакоммиченных/незапушенных правок:** выполни `git status --short` и проверку незапушенных коммитов (`git log --branches --not --remotes --oneline`), покажи список, напомни закоммитить/запушить. Не коммить сам без явной просьбы — только напоминание.
6. Спроси: «Что ещё зафиксировать?»

## Команда: /session-archive

Когда Денис пишет `/session-archive`:
1. Прочитай файл `SESSIONS-ARCHIVE.md`
2. Резюмируй ключевые моменты — решения, баги, паттерны
3. Спроси: «Что именно ищем?»

## Команда: /session-audits-archive

Когда Денис пишет `/session-audits-archive`:
1. Прочитай файл `AUDITS-ARCHIVE.md` (закрытые аудиты, задачи и находки, вынесенные из CLAUDE.md)
2. Резюмируй ключевые моменты — какие аудиты/находки закрыты, что было исправлено, важные паттерны
3. Спроси: «Что именно ищем?»

## Текущий фокус

**Этап:** 🆕 Сессия 54 — реализованы ВСЕ находки расширенного Fable-аудита графики CC-1..11 (красные CC-1..4 + жёлтые CC-5..11), по прямому выбору Дениса двумя заходами. Из аудита остался только вторичный пласт постов CC-12..15 (не выбраны). Коммиты — в репо **poigraem**: `7ddadf8` (CC-1: `?game=<id>` → новый `checkGameParam` вызывает `openGameModal` в `js/client.js`); в репо **content-creator**: `596d8bd` (CC-2 HD-обложки в батч-сторис через `_hdCoverUrl` + CC-3 чистка хвостов `PS4,PS5`/пробела-перед-двоеточием в `_displayTitle`), `4f8341b` (CC-4 цены в фигурных макетах коллажа — плашки верхних ячеек к внешнему краю у circ4/square5, чипы в углах секторов у circ4d), `c2d1a47` (CC-5 перекадровка обложки через `g.transform` в `_stImgRect`; CC-6 кламп зоны описания в холст; CC-7 композиция Веер/Неон при 2-3 играх; CC-8 описание внутри плашки, высота от контента; CC-9 `_stTextIsDark` меряет полосу под заголовком; CC-10 плашка-подложка при «Доработать в редакторе»; CC-11 горизонтальная раскладка OG-превью 1200×630). Все правки прошли `node --check` + численную проверку геометрии, но **визуально в браузере НЕ проверялись — приоритет следующей сессии**. Коммиты **НЕ запушены** (ждут отмашки).

🆕 **Репо-раскладка (деталь в шапке этого файла устарела):** `content-creator` теперь ОТДЕЛЬНЫЙ репозиторий (`github.com/enterteinment42/content-creator.git`), файл `content-creator.html` в КОРНЕ — НЕ `Standalone/content-creator/…` (коммит `54af20a` «Перенос из poigraem»). CC-1 правит основной магазин — репо `poigraem` (`js/client.js`). git-бинарь не в PATH → берётся у GitHub Desktop (`…/GitHubDesktop/app-<ver>/resources/app/git/cmd/git.exe`), identity в CLI задана локально per-repo (`Denis <spb.taximaxim@gmail.com>`), node — `C:\Program Files\nodejs\node.exe`. В `poigraem` есть чужие незакоммиченные правки — тронут строго `js/client.js`. Дрейфа в этой сессии не было.

⚠️ **Продолжает действовать урок сессии 49:** параллельная работа Дениса/другого агента в этом репо реальна и регулярна — git-операции в параллельном потоке иногда откатывают ВЕСЬ рабочий каталог, а не только правимый файл. Коммитить локально небольшими кусками СРАЗУ по готовности, не держать правки несохранёнными дольше пары минут.

_(Реестр готовых фич убран как дубль: детали закрытых сессий — в `SESSIONS-ARCHIVE.md` (`/session-archive`), закрытые находки UX-аудита — в `AUDITS-ARCHIVE.md` (`/session-audits-archive`), открытые находки UX — в разделе «🎨 UX-аудит» ниже.)_

**🟡 Открытые баги (жёлтые):**

Из аудита сессии 46 — все 4 исправлены по коду в сессии 52 (коммит `bc4a878`), НЕ проверены визуально:
- Toast «берутся первые 5 игр» → «доступны первые 5 игр (в кадре одновременно до 3)»
- Шаблон «Неон» теперь показывает ряд мини-обложек при `_stGameCount>1` (было — игнорировал, всегда 1 игра)
- «Горизонт» n>1: мини-обложки сдвинуты вверх и сужены — больше не перекрывают зону описания
- Убран двойной `display:none` в HTML IGDB-таба пикера

Из аудита 2026-07-02 (не исправлен):
- **Ж-11. `descModel` по умолчанию `'claude-sonnet-4-6'`** — тот же дефолт, что в `js/admin-core.js` основного магазина; `/api/claude` — чистый прокси к `api.anthropic.com` без валидации модели. Не трогали — нужна живая проверка генерации описаний (Денис не проверял пару недель).

**Ключевой факт об обложках:**
- `g.icon` в снапшоте может быть прямым IGDB URL — при скачивании IGDB не даёт CORS → `_safeImgUrl()` оборачивает в `/igdb/img?url=` прокси
- data URL (загрузка с ПК) нельзя добавлять `?_st=timestamp` — ломает формат → проверка `startsWith('data:')` перед кеш-бастингом

**Следующий шаг:**
0. 🆕 **Визуальная приёмка CC-1..11 на живом сайте (приоритет):**
   - CC-1: `poigraem.shop/?game=<id>` → открывается карточка игры (модалка); QR со сторис ведёт туда же.
   - CC-2: «📦 Батч по играм» → PNG чёткие (HD), не мыло.
   - CC-3: названия с `PS4,PS5`/двоеточием печатаются чисто на сторис/коллаже/посте.
   - CC-4: коллаж 5 игр в режиме «Карточки» на ⭕ Круг / ⭕ Круг-диаг / ◻️ Квадрат — цены видны, не перекрыты.
   - CC-5: в шаблонах сторис тянешь обложку/крутишь колесо → перекадровка работает (Постер/Диагональ/Горизонт/Неон/Карточки/Герой; Веер/Каскад/мини намеренно без этого).
   - CC-6: «Каскад» n=3 — описание не режется низом.
   - CC-7: «Веер»/«Неон» с 2-3 играми — меньше пустоты, мини-обложки читаемы. ⚠️ самые субъективные — смотреть внимательно.
   - CC-8: «Карточки»/«Герой» — описание внутри плашки под ценой, плашка не полупустая.
   - CC-9: «Горизонт» на светлой обложке — заголовок контрастный.
   - CC-10: «✏️ Доработать в редакторе» — под текстом тёмная плашка-подложка.
   - CC-11: коллаж «🔗 1200×630» — один ряд крупно, без чёрных полей по бокам.
1. 🆕 Запушить коммиты по отмашке (poigraem: `7ddadf8`; content-creator: `596d8bd`,`4f8341b`,`c2d1a47`).
2. **CC-12..15** (вторичный пласт постов, не выбраны): CC-12 (кликабельные ссылки в постах — разблокировано после CC-1), CC-13 (счётчик символов + экранирование Markdown), CC-14 (confirm перед отправкой в канал), CC-15 (колонка «Стор»→«Валюта»).
3. **Хвосты визуальной приёмки прошлых сессий** (код готов, в браузере не смотрели): сессия 53 (текстовый конвейер/батч/QR/TG-кнопка/мультиформат), Б-1..Б-4 (с.51), UX-15+старые жёлтые с.46 (с.52), batch B + зелёные UX-13/14/16-19 (с.49).
4. План роста сессии 50 пункты 2 (генератор заголовка поста) и 6 (кириллический self-host шрифт) — не выбраны, ждут решения.
5. Ж-11 — живой тест генерации описаний (`descModel='claude-sonnet-4-6'` подтверждён валидным, менять не нужно; тест не проведён).
6. После всего → Этап 2: вырезать коллаж/сторис/TG-пост из основного `index.html`.

## 📝 Аудит текстового конвейера и план роста (2026-07-07, сессия 50)

Полный аудит модели Fable по прямому запросу Дениса «как прокачать инструмент». Правок кода не было. Нумерация Б-N — новые баги, не пересекаются со старыми UX-N/Ж-N.

**Причина жалобы «заголовки как рандомные слова» (3 фактора, не баг ИИ):**
1. Названия игр идут в пост/подпись/картинку сырыми из PS Store-снапшота (`g.title` типа `Wolfenstein® II: The New Colossus™ (CUSA07378)`) — функции очистки `_cleanConsoleTitle`/`_cleanEditionTitle` (строки 740–767) в файле уже есть, но используются только для поиска в IGDB, не для отображения.
2. Все 3 ИИ-промпта (`generateAIPost:677`, `_stGenShortDescs:1437`, `_stGenCaption:1462`) шлют модели только список названий, без контекста — на малоизвестных играх модель угадывает содержание по словам в названии.
3. Промпт `_stGenShortDescs` жёстко просит «максимум ~12 слов» — на таком лимите получаются рубленые фразы-перечисления вместо цельных предложений.

**🔴 Новые баги — исправлены по коду (сессия 51, коммит `08a486e`, запушено), НЕ проверены визуально (по стандарту проекта ещё не закрыты):**
- ✅ **Б-1.** Потеря описаний из-за несовпадения ключей JSON во всех 3 ИИ-функциях (`generateAIPost`/`_stGenShortDescs`/`_stGenCaption`). *Сделано:* промпты шлют нумерованный список игр и просят JSON с ключами-индексами (`{"1": "...", "2": "..."}`); ответ маппится по позиции (`chunk[idx-1]`/`needed[idx-1]`/`missing[idx-1]`), а не по точному названию.
- ✅ **Б-2.** Тихий провал парсинга JSON в `generateAIPost`. *Сделано:* счётчик `lost` (игры без описания после генерации) → статус «..., без описания: N» + предупреждающий тост «⚠️ Пост готов, но N игр без описания» вместо ложного «✅ Пост готов».
- ✅ **Б-3.** Апостроф в `onclick="_pickerApply('...')"` мог сломать строку (HTML-сущности декодируются до исполнения JS). *Сделано:* клик по индексу в per-tab массивах `_pickerUrls={store,igdb,ps}` через `_pickerPick(tab,i)` — URL больше не попадает в onclick-атрибут во всех 3 вкладках пикера.
- ✅ **Б-4.** `openStory` грузил HD через IGDB для всех 5 игр, хотя рендерится максимум 3. *Сделано:* helper `_stUpgradeSlotHD(i)` с guard-флагом `_hdDone` и замыканием на объект игры; апгрейд только слотов `<min(len,3)` + до-апгрейд top-3 на reorder. В `_stG` по-прежнему до 5 игр — фича «выбрать какие 3 из 5 через ▲▼» сохранена, лёгкие 264px-обложки грузятся как раньше.

**Ж-11 закрыт по факту:** `descModel='claude-sonnet-4-6'` (строка 640) — подтверждён валидным активным ID (Sonnet 4.6) сверкой с актуальной справкой по Claude API. Менять не нужно. Живой тест генерации всё же не проводился.

**Рекомендованный план роста (по эффекту/трудозатратам):**
1. ✅ Текстовый конвейер: ключи-индексы (Б-1) + счётчик потерь (Б-2) + контекст из `g.description` в промпт + чистые названия для отображения (`_displayTitle` хелпер) + few-shot примеры в промпте сторис вместо жёсткого лимита слов — сделано сессия 51 (Б-1/Б-2) + сессия 53 (контекст+`_displayTitle`+few-shot), НЕ проверено визуально
2. ⬜ Генератор заголовка поста (кнопка «✨ Заголовок» — 3-5 вариантов хука от ИИ) + редактируемый вывод (`<textarea>` вместо `<pre>`) со счётчиком символов (лимиты TG: 1024/4096) — не реализовано, не выбрано Денисом
3. ✅ Прямая отправка поста в TG-канал — сделано сессия 53 (`backend/server.js` `POST /api/tg-post` + кнопка «📤 Отправить в канал»), эндпоинт задеплоен и подтверждён живым curl-тестом, кнопка в UI не проверена
4. ✅ Мультиформат экспорта (квадрат 1080×1080, превью-ссылка 1200×630) из готовых шаблонов — сделано сессия 53, НЕ проверено визуально
5. ✅ Батч-режим сторис (по одной на каждую выбранную игру), авто-контраст текста (хелпер `_sampleImgColor` уже есть), QR-код `?game=X` на сторис — сделано сессия 53, НЕ проверено визуально
6. ⬜ Кириллический self-host шрифт для заголовков (сейчас Rajdhani без кириллицы + Google Fonts нестабилен из РФ) — не реализовано, не выбрано Денисом
7. ✅ UX-15 (горячие клавиши) — сделано сессия 52, НЕ проверено визуально

**Что осталось:** пункты 2 и 6 плана роста ещё не реализованы и не выбраны Денисом; всё реализованное (1,3,4,5,7) ждёт визуальной приёмки.

---

## 🖼 Расширенный Fable-аудит графики (2026-07-13) — коллажи, сторис, посты, нумерация CC-N

Аудит по прямому запросу Дениса: «максимально задействовать возможности Fable, пока подписка активна» — фокус на графических инструментах (коллаж/сторис) как главной ценности инструмента, формирование постов — вторично. Отдельно отмечена идея кликабельных ссылок в названии игры в посте (зависит от карточек товара в магазине, которых пока нет — см. NEW-32 в основном `CLAUDE.md`).

**Методика (не только чтение кода):** поднят headless-Chrome стенд (puppeteer-core + системный Chrome) с реальным снапшотом (162 игры) — сгенерированы **27 настоящих PNG**: все 8 шаблонов сторис (n=1 и n=3, обычный и граничный набор с длинными названиями), все 7 макетов коллажа (режимы «Обложки»/«Карточки»), батч-экспорт, редактор с QR, оба мультиформата (1080×1080, 1200×630). Каждый PNG отсмотрен как арт-директор, не по коду. Прочитан также `backend/server.js` (`/api/tg-post`) и `js/client.js` основного магазина (обработка URL-параметров). Не дублирует закрытые UX-1..19/Б-1..4 (см. `AUDITS-ARCHIVE.md`). Правок кода не было — чистый аудит.

**Статус (сессия 54):** ✅ **CC-1..11 РЕАЛИЗОВАНЫ по коду** (коммиты — в «Текущем фокусе» выше), но **НЕ проверены визуально** — по стандарту проекта ещё НЕ закрыты, поэтому остаются здесь, а не в `AUDITS-ARCHIVE.md`. Осталось нетронутым (вторичный пласт постов, не выбрано Денисом): **CC-12, CC-13, CC-14, CC-15**.

**🔴 Критично (контент уходит подписчикам дефектным):**

- **CC-1. QR-код и вся идея «ссылки на карточку игры» ведут в никуда.** `stAddQR` генерирует `https://poigraem.shop/?game=<id>`, но основной сайт параметр `?game=` не обрабатывает вообще (`js/client.js` знает только `?order=<name>&price=<n>`, checkOrderParam). Подписчик сканирует QR → просто главная страница. Фикс: ~10 строк в `client.js` основного сайта — обработчик `?game=<id>` → найти игру по id → `openGameModal`. Разом оживит QR **и** откроет дорогу ссылкам в постах (CC-12).
- **CC-2. Батч-экспорт сторис (`downloadStoryBatch`) — мыльные картинки.** В отличие от интерактивного `openStory`, батч не вызывает `_hdCoverUrl` — грузит только `g.icon` (440px). Сравнение `st-poster-n1-A.png` (чёткий, интерактивный режим) vs `st-poster-n1-BATCH-noHD.png` (мыло) — разница драматическая. Главный инструмент массового производства выдаёт худшее качество. Фикс: `await _hdCoverUrl(entry.title)` в цикле батча с фолбэком на icon.
- **CC-3. Мусорные хвосты названий на самих картинках.** `_displayTitle` не снимает хвост `PS4,PS5` (запятая перед ним, регэксп ждёт пробел) и оставляет « :» после вырезания `™`. Подтверждено исполнением на реальных строках снапшота: `GreedFall - Standard Edition PS4,PS5`, `Mortal Shell: Enhanced Edition PS4,PS5`, `LEGO Star Wars : The Force Awakens` — печатаются как есть на сторис/коллажах/постах (41 игра в снапшоте с хвостом `PS4,PS5`). Тот же класс, что UX2-2 основного сайта. Фикс: `.replace(/[,\s]+(PS[45][\s,&]*)+$/i,'')` + `.replace(/\s+:/g,':')`.
- **CC-4. Фигурные макеты коллажа ломают режим «Карточки с ценами».** На `circ4`/`square5` центральный круг/квадрат перекрывает плашки соседних карточек (обложка/цена обрезаны). `circ4d` в режиме карточек вообще не рисует ни одной цены (`_redrawCirc4d` возвращается до отрисовки оверлеев) — режим молча превращается в «Обложки». Фикс: сдвигать плашку к внешнему краю ячейки для circ4/square5; для circ4d — дорисовать цены в углах либо блокировать сочетание с подсказкой.

**🟡 Важно (качество/полнота графики):**

- **CC-5. В шаблонах сторис нельзя перекадрировать обложку — перетаскивание молча не работает.** `_stImgRect` кропит строго по центру и игнорирует `g.transform`; мышиные хендлеры в режиме шаблонов честно меняют `transform`, но перерисовка их не учитывает (мёртвое взаимодействие). На `st-cards-n3-A.png` у обложки FC26 срезаны головы футболистов — в шаблоне это не исправить. Фикс с большой отдачей: учесть `t.ox/t.oy/t.scale` внутри `_stImgRect` (как уже сделано в `_stDrawBg` редактора) — все 8 шаблонов сразу получат ручную перекадровку.
- **CC-6. «Каскад» n=3: зона описания вылезает за холст.** descY + высота бара выходит за 1920px — на `st-cascade-n3-A.png` вторая строка описания обрезана краем файла. Хвост UX-9. Фикс: `descY = Math.min(descY, ch - barH)`, высоту бара измерять до отрисовки.
- **CC-7. «Веер» и «Неон» с 2-3 играми: композиция разваливается.** Веер — ~40% холста пустая чернота сверху/снизу, glow-фон почти невидим. Неон n=3 — мини-обложки ~97×125px, цены при них нечитаемы. Фикс: веер — карточки крупнее и инфоблок выше; неон — мини-обложки минимум `ch*.11`.
- **CC-8. «Герой»/«Карточки»: плашка на 30% слота полупустая, описание висит отдельным блоком над ней, а не внутри.** На `st-hero-n3-A.png` под ценой ~180px мёртвой темноты, бар описания «плавает» отдельно, разрывая композицию. Фикс: рисовать описание внутри плашки после цены, высоту плашки — от контента.
- **CC-9. Авто-контраст «Горизонта» меряет всю обложку, а не зону под текстом.** `_stTextIsDark` — средняя яркость всей картинки. На `st-horizon-n3-A.png` заголовок стоит на светлой футболке (локально светло), но глобально обложка тёмная → выбран белый текст → заголовок тонет. Фикс: сэмплировать горизонтальную полосу под заголовком, а не весь кадр.
- **CC-10. «✏️ Доработать в редакторе» теряет плашки — раскладка выглядит сломанной.** `_stTemplateToEditor` создаёт title-оверлей с `bgColor:'transparent'` и цену без подложки — на `st-editor-QR.png` тексты плавают по картинке без тёмной зоны; редактор к тому же затемняет фон (0.38), а шаблон — нет, «до/после» не совпадают. Фикс: добавлять первым элементом overlay-плашку `rgba(8,8,20,0.92)` на нижние 30% слота.
- **CC-11. Формат 1200×630 (OG-превью) бесполезен в текущем виде.** `downloadCollageFormat` вписывает вертикальную композицию в горизонтальный кадр — на `coll-og-1200x630.png` гигантские чёрные поля по бокам, текст нечитаем в размере превью. Фикс: для OG собирать отдельную горизонтальную раскладку (1 ряд карточек крупно), а не масштабировать вертикальную.

**Посты (вторичный приоритет):**

- **CC-12. Кликабельные ссылки в названиях игр — почти бесплатно, инфраструктура уже готова.** Бэкенд `POST /api/tg-post` (`backend/server.js:569`) уже шлёт `parse_mode:'Markdown'` — ссылки `[Название](url)` в тексте поста уже рендерятся кликабельными (подвал поста так и работает сейчас). `generateAIPost` уже держит `g.id` на руках — обернуть название в markdown-ссылку — 1 строка. Единственный блокер — CC-1 (сайт не знает `?game=`). Временный обходной путь через уже существующий `?order=<name>&price=<n>` работает уже сегодня, но кликом сразу кладёт игру в корзину — слишком агрессивно для ссылки внутри описания. **Рекомендация: чинить CC-1 и CC-12 одним заходом (~20 строк суммарно).**
- **CC-13. Пост длиннее 4096 символов молча упадёт при отправке.** Счётчика символов нет, клиент длину не проверяет — 12-15 игр с описаниями легко превышают лимит `sendMessage`. Заодно спецсимволы Markdown v1 (`[`, `_`, `*`) в названии/описании → «can't parse entities», весь пост не уходит. Фикс: счётчик символов под текстом поста + предупреждение >4096; экранировать `[_*` в названиях/описаниях перед вставкой.
- **CC-14. «📤 Отправить в канал» — необратимая публикация без подтверждения.** Один случайный клик — пост в живом канале, кнопка стоит рядом с «🔄 Заново». Фикс: двухшаговое подтверждение в самой кнопке (паттерн уже реализован в корзине основного магазина, сессия 76).
- **CC-15 (мелочь).** Колонка «Стор» в таблице показывает валюту закупки (TRY/INR), а не платформу игры — вводит в заблуждение при беглом взгляде. Переименовать в «Валюта» или скрыть за ненадобностью.

**Что подтверждено хорошим (не трогать):** «Постер» n=1 и «Неон» n=1 — уровня приличного паблика; «Горизонт» — лучший мультиигровой шаблон; коллажи 2+3/3+2 в режиме карточек — выглядят профессионально; режим «Обложки» чистый без артефактов; `_wrapLines` работает корректно (длинные названия переносятся с «…»); HD-конвейер в интерактивном режиме честно дощёлкивает чёткость; QR технически грузится и сканируем (сама генерация не сломана — сломана только целевая ссылка, см. CC-1); цепочка «выбрал игры → собрал пост → отправил в канал» — 4 клика; таблица игр с сортировкой/поиском удобна.

**Приоритизация (эффект/трудозатраты):**

| (а) Максимальный визуальный/творческий скачок | (б) Полировка — можно отложить |
|---|---|
| 1. CC-1 + CC-12 — `?game=` на сайте + ссылки в постах + живой QR (один заход, ~20 строк, оживляет 3 фичи разом и даёт новый канал трафика с постов/сторис на карточки товара) | CC-9 (локальный авто-контраст «Горизонта») |
| 2. CC-2 — HD-обложки в батче (3 строки, поднимает качество всего массового контента) | CC-10 (плашки при переносе шаблона в редактор) |
| 3. CC-3 — чистка мусорных хвостов названий (2 регэкспа, видно на каждом PNG) | CC-11 (горизонтальная раскладка для OG-формата) |
| 4. CC-5 — перекадровка обложки в шаблонах (одна функция, закрывает разом все 8 шаблонов) | CC-14 (confirm перед отправкой в канал) |
| 5. CC-6 + CC-4 — обрезанное описание в «Каскаде» + конфликты фигурных макетов коллажа | CC-7/CC-8 (композиция «Веер»/«Неон»/«Герой»), CC-13 (счётчик символов поста), CC-15 |

**Что осталось:** CC-1..11 сделаны (сессия 54), ждут визуальной приёмки. Не тронуто: CC-12..15 (вторичный пласт постов) — ждут решения Дениса.

---

## 🎨 UX-аудит (2026-07-02) — удобство и качество конечного контента

Сплошное чтение всех 1517 строк глазами пользователя и с прицелом на итоговый PNG. Нумерация UX-N. **Статус:** проверены на живом сайте (сессия 48) и закрыты — UX-1..5, UX-7, UX-9, UX-10 → перенесены в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`). Ниже остаются ОТКРЫТЫЕ: исправлены по коду, но НЕ проверены визуально — UX-6, UX-8, UX-11, UX-12, UX-15 (жёлтые/UX-15), UX-13, UX-14, UX-16, UX-17, UX-18, UX-19 (зелёные). Не дублирует старые Ж-баги (Неон, тост «первые 5», Горизонт, display:none) — они остаются отдельно, см. раздел «Открытые баги» ниже (тоже исправлены в сессии 52, не проверены визуально).

**🟡 Жёлтые (исправлены по коду, ждут визуальной проверки):**

- ✅ **UX-6. Макеты коллажа существуют только для ровно 5 игр.** ИСПРАВЛЕНО (`33e0790`, НЕ проверено визуально): `getCells()` теперь считает отдельные раскладки для n=2 (2 кадра рядом), n=3 (1 большой + 2 малых), n=4 (2×2) — до этого при n<5 всегда рисовалась лента-ряд независимо от выбранного макета. Радио-макеты теперь блокируются (тускнеют + подсказка) когда игр не ровно 5. Было: все 7 макетов требуют `n>=5`; для 2–4 игр молчаливый фолбэк «все в ряд» → PNG-лента 636…1272×424.
- ✅ **UX-8. Названия обрезаются в одну строку посимвольно во всех шаблонах.** ИСПРАВЛЕНО (`9e89ddb`, НЕ проверено визуально): общий хелпер `_wrapLines(ctx,text,maxW,maxLines)` — перенос на ≤2 строки с «…» на второй при переполнении. Применён во всех 8 местах: коллаж-карточки (`_drawCardOverlay`) + 7 шаблонов сторис (`_stDrawCoverCard`/Poster/Fan/Diag/Cascade/Horizon/Neon). Цена/бейдж/зона описания сдвигаются под реальную высоту заголовка (1 или 2 строки), а не жёстко под одну.
- ✅ **UX-11. Логотип: жёсткий кружок 80px + 5 штампов в коллаже.** ИСПРАВЛЕНО (`1a31cd6`, НЕ проверено визуально): новая опция «Форма» (Кружок/Плашка/Как есть — последний вписывает лого с сохранением пропорций без обрезки) в коллаже и сторис, общий `_drawLogoBox()`. В коллаже-«карточках» логотип теперь рисуется один раз поверх всего холста (`_drawCollageWatermark`) вместо штампа в каждую из 5 ячеек.
- ✅ **UX-12. Превью ≠ экспорт по переносам текста.** ИСПРАВЛЕНО (`9e89ddb`, НЕ проверено визуально): убран `Math.round()` вокруг размеров шрифта заголовка/описания во всех местах, где он влиял на `measureText` (canvas поддерживает дробные px) — именно округление давало разное соотношение при разных масштабах canvas (маленькое превью vs 2×/1080 экспорт). Второстепенные отступы/паддинги (`pad=Math.round(24*sx)` и т.п.) намеренно не трогали — эффект на перенос слов у них на порядок меньше.

**🟢 Зелёные (удобство, идеи):**

- ✅ **UX-13.** ИСПРАВЛЕНО (`e4f2aa9`, НЕ проверено визуально): кнопка «Снять всё» (сбрасывает `_sel` у ВСЕХ игр, не только видимых) + чекбокс «Только выбранные» (фильтрует таблицу под текущий выбор независимо от поиска).
- ✅ **UX-14.** ИСПРАВЛЕНО (`e4f2aa9`, НЕ проверено визуально): клик по заголовку «Цена»/«Скидка» сортирует (▲/▼ индикатор, повторный клик — реверс); новая колонка 📝 — ✓ зелёным если есть `g.description`, иначе тусклый «–».
- ✅ **UX-15.** ИСПРАВЛЕНО (`0e97e11`, сессия 52, НЕ проверено визуально): глобальный `keydown` — Esc закрывает открытую модалку (коллаж/сторис/TG); в редакторе сторис Del/Backspace удаляет выделенный элемент (вне полей ввода), Ctrl+Z/Cmd+Z откатывает через стек `_stUndoStack` (глубина 30).
- ✅ **UX-16.** ИСПРАВЛЕНО (`2a6a9c7`, НЕ проверено визуально): `_sampleImgColor(img)` сэмплирует пиксели загруженной обложки (24×24 offscreen canvas, пропускает серые/тёмные/пересвеченные — ищет насыщенный акцент), кэш по `img.src`; хэш от названия остался фолбэком, если картинка ещё не загружена или canvas затейнтован (try/catch).
- ✅ **UX-17.** ИСПРАВЛЕНО (`5754258`, НЕ проверено визуально): `GENRE_EMOJI` — таблица regex-паттернов по названию+описанию (гонки→🏎️, шутеры→🔫, хорроры→👻, RPG-фэнтези→🐉, спорт→🏆 и т.д.), хэш-пул остался фолбэком для нераспознанных.
- ✅ **UX-18.** ИСПРАВЛЕНО (`623f417`, НЕ проверено визуально): переключатель TG/VK уменьшен до компактной пилюли с подписью «Для:» + tooltip «влияет только на кнопку «Текст к сторис» ниже» — было первым и самым крупным элементом панели.
- ✅ **UX-19.** ИСПРАВЛЕНО (`623f417`, НЕ проверено визуально): ширина мини-обложки в «Диагонали» ограничена `miniH*.78` и центрируется — раньше при n=2 единственная мини-обложка растягивалась на всю ширину холста (1080×288).

## Лог сессий

### 2026-07-13 — Весь расширенный Fable-аудит графики CC-1..11 реализован (сессия 54)

**Контекст:** прямое продолжение аудита CC-1..15. Денис сначала уточнил CC-1 («на сайте нет карточек игр?») — проверкой `poigraem/index.html`+`js/client.js` выяснили: карточка (`#game-modal`+`openGameModal(gid)`) есть и универсальна, но точка входа в неё была только у витрины (featured/new carousel), а основной список (`renderClientList`) раскрывает описание на месте (`toggleDesc`) без модалки. Ключевое: `openGameModal` работает по данным из `games[]`, не завязан на carousel → `?game=<id>` откроет модалку для ЛЮБОЙ игры, обычный UI списка не меняется. Затем Денис дал делать двумя заходами: сначала CC-1+CC-2+CC-3+CC-4, потом CC-5..11.

**Что сделано (все закоммичены локально, НЕ запушены):**
- **CC-1** (репо poigraem, `7ddadf8`, только `js/client.js`): новый `checkGameParam()` (по образцу `checkOrderParam`) — при заходе с `?game=<id>` находит игру в снапшоте и открывает `openGameModal`, чистит адрес. Вызывается после `renderClientList`/`checkOrderParam`. Оживляет QR-код сторис и открывает дорогу ссылкам в постах (CC-12).
- **CC-2** (content-creator, `596d8bd`): `downloadStoryBatch` тянет HD-обложку через `_hdCoverUrl` (как интерактивный `openStory`), icon 440px — фолбэк на ошибке загрузки. Массовый экспорт больше не мыло.
- **CC-3** (тот же коммит): `_displayTitle` + `.replace(/[,\s]+(PS[45][\s,&]*)+$/i,'')` (хвост `PS4,PS5` с запятой) + `.replace(/\s+:/g,':')` (пробел перед двоеточием после вырезанного ™). Прогнано на реальных строках снапшота.
- **CC-4** (`4f8341b`): режим «Карточки» на фигурных макетах. `_drawCardOverlay` получил параметр `edge` ('top'/'bottom') — у circ4/square5 плашки верхних ячеек уходят к внешнему (верхнему) краю (центр-фигура их не перекрывает), градиент/акцент разворачиваются. Для circ4d (раньше цены не рисовались вообще) — хелперы `_chipSize`/`_drawFigPriceChip` рисуют чип цены+скидки в углу экрана каждого сектора (угол детектится по вершине полигона `_pinwheelPolys`), центральная игра — чип на нижней кромке круга. Численно подтверждено: каждый сектор содержит ровно один экранный угол.
- **CC-5..11** (`c2d1a47`, один файл): CC-5 `_stImgRect` учитывает `g.transform` (ox/oy через глобальный sx/sy, scale) → перекадровка обложки в шаблонах ожила (drag/колесо уже вызывали `_stRenderTemplate`, но перерисовка игнорила transform); transform передан в Постер/Диагональ/Горизонт/Неон-главную и `_stDrawCoverCard` (Карточки/Герой), Веер/Каскад/мини оставлены center-crop (drag-слот не совпадает с их визуальной раскладкой). CC-6 `_stDrawDescZone` клампит `y` внутри холста. CC-7 Веер — карточки крупнее (.46-.56)/выше (.34), инфоблок .62, описание .80; Неон n>1 — главная обложка меньше (.56)/выше (.13), мини крупнее (.12 высоты), цена .76. CC-8 `_stDrawCoverCard` — высота плашки от контента, короткое описание ВНУТРИ плашки под ценой (Карточки/Герой больше не зовут `_stDrawDescZone`). CC-9 `_stTextIsDark(img,y0,y1)` меряет полосу 0.36-0.52 под заголовком, вызов в «Горизонте». CC-10 `_stTemplateToEditor` пушит первым тёмную плашку-подложку `rgba(8,8,20,.92)` на нижние 30% слота. CC-11 `downloadCollageFormat` для горизонтального формата (f.w>f.h → OG 1200×630) собирает раскладку в один ряд вместо вписывания вертикали.

**Что решили:**
- CC-1 формулировка в аудите была неточной («нет карточек, некуда вести») — карточка есть (SPA-модалка), не заведён только вход по URL-параметру. Судьбу `?game=` оставили как открытие модалки (Денис подтвердил, что анимация списка не меняется).
- CC-4 — по выбору Дениса вариант «Плашки к внешнему краю» (не «чипы везде» и не «блокировать сочетание»).
- CC-5 — transform только там, где drag-слот совпадает с визуальным слотом (полнокадровые/вертикальные), иначе панорама уводила бы не ту игру.
- Верификация — `node --check` + численная проверка геометрии (Neon/Fan n=3 без коллизий, cardH CC-8 в пределах слота, углы circ4d). Puppeteer-стенд из аудита уже снят, заново не поднимал — визуальная приёмка за Денисом.

**Новые баги:** не найдено (реализация аудита).

**Открытые вопросы:** ничего из CC-1..11 не отсмотрено в браузере (приоритет след. сессии); CC-7/CC-8 самые субъективные.

**Что осталось:**
1. Визуальная приёмка CC-1..11 (детали — «Следующий шаг» вверху) + пуш коммитов по отмашке.
2. CC-12..15 (вторичный пласт постов) — не выбраны.
3. Хвосты приёмки сессий 53/51/52/49; план роста 2&6; Ж-11; Этап 2.

### 2026-07-12 — План роста доделан: текстовый конвейер + батч/QR/контраст + отправка в TG + мультиформат (сессия 53)

**Контекст:** прямое продолжение сессии 52. Денис выбрал порядок реализации оставшихся пунктов плана роста сессии 50: сначала пункт 1 (текстовый конвейер) до конца, затем 5, 3, 4 (батч-сторис+QR, отправка в TG, мультиформат экспорта). Пункты 2 (генератор заголовка) и 6 (кириллический шрифт) не выбраны.

**Что сделано:**
- **Пункт 1 — текстовый конвейер добит до конца** (коммиты `ce921ee`, `cc22e6e`, `3ee64f8`): новый хелпер `_displayTitle(s)` — лёгкая чистка названия для отображения (убирает ®/™/©, коды вида `(CUSA07378)`, приписки платформы `[PS4 & PS5]`/`(PlayStation®5)`/трейлинг `PS4/PS5/Xbox/Version`), в отличие от `_cleanEditionTitle` НЕ обрезает Deluxe/Bundle/Collection — это часть названия. Применён в `collageCovers`/`_stG` (центральные точки — покрывает разом ВСЕ 8 шаблонов сторис и коллаж без правки каждого шаблона отдельно), в админ-таблице (`td-title`), в промпте и выводе `generateAIPost`. `_stGenShortDescs` теперь передаёт существующее `g.description` как контекст в промпт («сократи до сути, не выдумывай») + добавлены 2 few-shot примера вместо жёсткого лимита «~12 слов» — раньше эта функция игнорировала уже готовое описание игры и всегда угадывала по одному названию, даже когда описание уже было.
- **Пункт 5 — батч-сторис + QR + авто-контраст** (коммиты `4ccab11`, `076beab`, `8cc7f41`): в `_stG` добавлено поле `id:g.id` (из снапшота, нужно для QR-ссылок). `_stTextIsDark(img)` — сэмплирует среднюю яркость обложки (16×16 offscreen canvas, кэш по `img.src`) — применено в шаблоне «Горизонт» (единственный, где заголовок стоит прямо на сырой картинке без тёмной подложки, только вигнетка) — заголовок сам переключается на тёмный текст+обводку на светлых обложках. Новый тип элемента `qr` в редакторе сторис — кнопка «🔗 QR-код», `stAddQR()` берёт игру активного слота, генерирует `https://poigraem.shop/?game=<id>`, тянет QR-картинку через `api.qrserver.com`, обёрнутую в тот же VPS-прокси `_safeImgUrl` (у qrserver нет CORS-заголовков для `<img>`), рисуется с белой подложкой для сканируемости. Новая функция `downloadStoryBatch()` (кнопка «📦 Батч по играм», только режим шаблонов — в редакторе элементы расставлены вручную под конкретную игру и не переносятся) — по одной PNG на каждую выбранную игру (кап 15), с паузой 400мс между автозагрузками, использует текущий шаблон/стиль/лого; по завершении восстанавливает исходное состояние `_stG`/канвы.
- **Пункт 3 — прямая отправка в TG-канал** (коммит `backend/server.js` `ce632be` + `content-creator.html` тот же коммит): новый эндпоинт `POST /api/tg-post` в основном бэкенде магазина (`backend/server.js`, НЕ price-lookup) — защищён `requireAdmin` (`Authorization: Bearer <ADMIN_TOKEN>`, тот же токен что уже хранится в `localStorage['cc_admin_token']` для вкладки PS Store пикера), шлёт `{text}` через уже настроенный локальный `telegram-bot-api` (`TG_API_BASE`, дефолт `http://localhost:8081`) с `TG_BOT_TOKEN` в канал «Поиграем?» — `chat_id: -1002347087956` (`@play_best_games`, узнан у Дениса напрямую, не гадался). Кнопка «📤 Отправить в канал» в панели готового поста — если токена нет в localStorage, спрашивает через `prompt()` один раз и сохраняет. **Задеплоено на VPS отдельным агентом** (пошаговая инструкция была подготовлена и передана Денису) — `git pull` + `pm2 restart poigraem-api`, дрейф перед перезаписью проверен (расхождение было только в отсутствующем блоке `/api/tg-post`), живой `curl`-тест успешен (`{"ok":true,"message_id":4390}`, сообщение реально пришло в канал), бот уже был админом канала — донастройка не понадобилась.
- **Пункт 4 — мультиформат экспорта** (коммит `13ccc5e`): `downloadCollageFormat(fmt)` + кнопки «◻️ 1080×1080» (VK/Instagram) и «🔗 1200×630» (Open Graph превью-ссылка) в модалке коллажа. Реализовано БЕЗ дублирования логики отрисовки: та же композиция коллажа рендерится на промежуточный канвас в родном разрешении (как в обычном `downloadCollage`, scale=2), затем вписывается (contain, по центру) в целевой канвas фиксированного размера поверх фона, отрисованного через уже существующий `_drawCollageBg`.

**Что решили:**
- `_displayTitle` применяется в ДВУХ центральных точках построения `_stG`/`collageCovers`, а не в каждом из 8 шаблонов отдельно — де-факто наименее рискованный способ покрыть всё сразу.
- QR через сторонний `api.qrserver.com`, а не самописный QR-энкодер в одном HTML-файле — оправдано низким риском (публичные данные — просто URL игры) и кардинально меньшим объёмом кода; картинка всё равно идёт через свой VPS-прокси, так что зависимость от внешнего домена ограничена только генерацией самого QR-кода.
- «Авто-контраст текста» из плана — намеренно НЕ применён ко всем шаблонам: у Fan/Diag/Cascade/Neon заголовок всегда рисуется на синтетическом тёмном фоне/градиенте (`_stRenderTemplateBg`), не на сырой обложке, так что риска нет; только «Горизонт» кладёт текст прямо на картинку.
- Батч-экспорт работает только в режиме «Шаблоны» — в «Редакторе» текст/цена привязаны к конкретной игре вручную, автоматический перенос на другую игру был бы некорректным и не проще было бы предупреждать пользователя, чем явно ограничить кнопку.
- `chat_id` канала — числовой (`-1002347087956`), не `@username` — по прямому выбору Дениса (надёжнее на случай приватизации канала), хотя технически `@username` тоже сработал бы для публичного канала.

**Новые баги:** не найдено (сессия реализовывала запланированное).

**⚠️ Инциденты с дрейфом (тот же паттерн, что в сессии 49):**
- Во время правки текстового конвейера `content-creator.html` частично откатился до последнего коммита прямо посреди серии правок — из 5 запланированных изменений уцелело только последнее (проверено `git diff --stat`: 4 вставки вместо ожидаемых ~15). Восстановлено переприменением поверх актуального состояния диска, с коммитом после КАЖДОГО отдельного изменения вместо одного большого коммита в конце — так пережили ещё несколько последующих чужих коммитов (`avito-helper`, `js/client.js` от параллельной сессии) без потерь.
- Сама `CLAUDE.md` тоже откатилась до состояния сессии 51 в процессе диалога — пропала целиком запись сессии 52 (UX-15 + старые жёлтые сессии 46). Восстановлена в этом `/session-end` по содержимому, которое было прочитано в самом начале этого диалога (до отката) — см. новую запись «сессия 52» ниже.
- Отдельно поймана временная недоступность классификатора безопасности инструмента PowerShell (не связана с проектом/дрейфом) — блокировала `node --check`/`git commit` на несколько попыток подряд, самостоятельно восстановилась.

**Открытые вопросы:**
- Ничего из content-creator.html в этой сессии не проверено в браузере — приоритет следующей сессии (см. «Следующий шаг» вверху).
- Кнопка «📤 Отправить в канал» проверена только косвенно (бэкенд подтверждён curl'ом) — сам клик в UI не тестировался.

**Что осталось:**
1. Визуальная приёмка всей сессии 53 (детали — в «Следующий шаг» вверху файла)
2. Хвосты сессий 51/52/49 не сдвинулись — визуальная приёмка Б-1..Б-4, UX-15+старые жёлтые, batch B+зелёные UX-13/14/16-19
3. Пункты 2 и 6 плана роста (генератор заголовка поста, кириллический шрифт) — не реализованы, ждут решения Дениса
4. Ж-11 — живой тест генерации описаний так и не проведён
5. После всего → Этап 2: вырезать коллаж/сторис/TG-пост из основного `index.html`

### 2026-07-10 — UX-15 (горячие клавиши) + все 4 старых жёлтых бага сессии 46 (сессия 52)

**Контекст:** прямое продолжение сессии 51. Денис выбрал из списка оставшихся задач сначала UX-15, затем «пункт 5» — старые жёлтые баги сессии 46. Модель по ходу переключена на Opus 4.8.

**Что сделано:**
- **UX-15 (коммит `0e97e11`):** добавлен глобальный `document.addEventListener('keydown', …)` после collage-listeners. **Esc** — закрывает открытую `.modal-bg.open` (диспетчер по id: `closeCollage`/`closeStory`/`closeTGPost`). **Del/Backspace** — `stDeleteSel()`, только когда открыт story-modal в режиме `editor`, есть выделение (`_stSel>=0`) и фокус НЕ в input/textarea/contentEditable (guard `typing`). **Ctrl+Z / Cmd+Z** — новый `stUndo()`. Введён стек `_stUndoStack` (глубина 30): `_stSnapshot()` кладёт глубокую копию `_stEls` + `_stSel` и вызывается в начале `stAddOverlay/stAddPrice/stAddSticker/stAddLogo/stDeleteSel/stDuplicateSel/stMoveUp/stMoveDown`; сбрасывается в `openStory`. Подсказка на канвасе дополнена «Del = удалить · Ctrl+Z = отменить», у кнопки ✕ добавлен `title="Удалить (Del)"`.
- **Старые жёлтые сессии 46 (коммит `bc4a878`):**
  - Тост в `openStory` «Для сторис берутся первые 5 игр» → «Для сторис доступны первые 5 игр (в кадре одновременно до 3)» — честно про реальный лимит рендера (макс. 3).
  - `_stTplNeon` игнорировал `_stGameCount>1` — добавлены `const n=Math.min(_stGameCount,_stG.length,3)` и блок `if(n>1)`: ряд из 1-2 мини-обложек с ценой и неоновой рамкой в свободном окне между главной обложкой (низ `covY+covH`) и главной ценой (`ch*.84`).
  - `_stTplHorizon` n>1: мини-обложки сдвинуты вверх (`my: ch*.70→.62`) и сужены (`.30/.24 → .26/.20` ширины), чтобы блок мини-обложек + их цены заканчивался выше зоны описания (`ch*.88`), а не налезал на неё.
  - Убран дубль `display:none` в inline-стиле `#cc-picker-igdb` (был `display:none;padding:10px;display:none;flex-direction:column`), сверен с корректным соседним `#cc-picker-ps`.
- **Верификация:** `node --check` (извлечённый inline-скрипт) чист после каждого коммита; геометрия Neon/Horizon просчитана в node для n=2 и n=3 — все вертикальные зазоры положительные (Horizon: мини↔описание 93/180px; Neon: обложка↔мини 14px, мини-цена↔главная цена 29px, главная цена↔описание 39px), по горизонтали оба влезают в 1080.

**Что решили:**
- Undo сделан как стек снапшотов только над `_stEls` (элементы редактора), не над трансформами обложек/шаблонами — покрывает ровно операции add/delete/duplicate/move, где риск случайной потери выше всего. Глубина 30 достаточна, память ничтожна.
- Del/Ctrl+Z активны только в режиме `editor` и вне полей ввода — чтобы Backspace при редактировании текста описания не удалял элемент (частый антипаттерн).
- Neon: мини-обложки поставлены между обложкой и ценой (там естественное пустое окно ~190px), а не снизу как в Horizon — снизу у Neon уже стоит крупная цена.
- Коммит строго по пути `Standalone/content-creator/content-creator.html`; чужой дрейф `avito-helper.html` в рабочем дереве не трогал.

**Новые баги:** не найдено (реализация известных задач).

**Открытые вопросы:**
- Ничего из сессии 52 не проверено визуально на живом сайте — только `node --check` + численный расчёт геометрии. Приоритет следующей сессии.

**Что осталось:**
1. Визуальная приёмка UX-15 (Esc/Del/Ctrl+Z, Backspace в textarea не удаляет) + жёлтых с.46 (Неон/Горизонт с 2-3 играми, тост, IGDB-таб пикера)
2. Прежние хвосты: визуальная приёмка Б-1..Б-4 (сессия 51) и batch B + зелёных (сессия 49)
3. План роста сессии 50 пункты 2-6; Ж-11 (живой тест генерации)
4. Этап 2: вырезать коллаж/сторис/TG-пост из основного `index.html`

### 2026-07-10 — Фикс всех красных багов Б-1..Б-4 + диета CLAUDE.md (сессия 51)

**Контекст:** Денис попросил (1) провести диету `CLAUDE.md` по гайду `../../CLAUDE-MD-DIET-GUIDE.md`, затем (2) собрать все незакрытые красные баги Content Creator и чинить. Красными оказались только Б-1..Б-4 из аудита сессии 50 (красные UX-1..4 уже закрыты и в архиве).

**Что сделано:**
- **Диета `CLAUDE.md` (коммит `cc86951`, 80.4→57.1 КБ):** лог сессий 10→5 (сессии 45,44,43,42,37 перенесены дословно в начало `SESSIONS-ARCHIVE.md`); создан `AUDITS-ARCHIVE.md`, куда вынесены закрытые находки UX-аудита (UX-1..5, UX-7, UX-9, UX-10 — проверены на живом сайте в сессии 48); в разделе «🎨 UX-аудит» остались только открытые (UX-6/8/11/12 + зелёные + UX-15), шапка аудита переписана. Убраны прозаические дубли в «Текущем фокусе» (реестр «Что готово» и пересказ сессии 49 — всё сохранено в архивах/логе). `/session-end` обновлён (порог 5, перенос закрытого в `AUDITS-ARCHIVE.md`, git-напоминание), добавлена команда `/session-audits-archive`.
- **Б-1 — потеря описаний из-за ключей JSON (все 3 ИИ-функции), коммит `08a486e`:** `generateAIPost`/`_stGenShortDescs`/`_stGenCaption` теперь нумеруют игры в промпте (`1. Title`) и просят JSON с числовыми ключами-индексами (`{"1":"..."}`); ответ маппится по позиции (`chunk[idx-1]`/`needed[idx-1]`/`missing[idx-1]`), а не по точному совпадению названия — ®/™/пробелы больше не роняют описание.
- **Б-2 — тихая потеря в `generateAIPost` (тот же коммит):** добавлен счётчик `lost` (игры без описания после генерации, ловит и битый JSON-пакет, и промах ключа) → статус «..., без описания: N» + тост «⚠️ Пост готов, но N игр без описания» вместо ложного «✅».
- **Б-3 — апостроф в `onclick` пикера (тот же коммит):** URL обложек больше не подставляется в атрибут. Введены per-tab массивы `_pickerUrls={store,igdb,ps}` и диспетчер `_pickerPick(tab,i)` — клик по индексу, во всех 3 вкладках пикера. Раздельные массивы исключают промах индекса при переключении вкладок.
- **Б-4 — лишние IGDB-запросы в сторис (тот же коммит):** helper `_stUpgradeSlotHD(i)` с guard-флагом `_hdDone` и замыканием на объект игры (переживает reorder, попутно чинит скрытую гонку старого кода); `openStory` апгрейдит только слоты `<min(len,3)`, `stReorderGame` до-апгрейдит top-3. В `_stG` осталось до 5 игр — фича «выбрать какие 3 из 5 через ▲▼» сохранена.

**Что решили:**
- Красные Б-1..Б-4 не переносить в `AUDITS-ARCHIVE.md`: код готов и запушен, но визуально на живом сайте не проверялся — по стандарту проекта это ещё НЕ закрытие. В шапке аудита помечены «исправлены по коду, ждут визуальной проверки».
- Б-4 чинить БЕЗ наивного `slice(0,3)`: он удалил бы игры #4/#5 и сломал reorder-выбор. Правильный фикс — грузить HD только для видимых, но держать 5 в `_stG`.
- Коммитить по логическим кускам и сразу пушить — из-за параллельной работы в репо (риск потери, как в сессии 49). Заливка строго по путям `Standalone/content-creator/` — чужие правки в корне и price-lookup не тронуты.

**Новые баги:** не найдено (сессия чинила известные + документация).

**Открытые вопросы:**
- Ничего из Б-1..Б-4 не проверено визуально на живом сайте — приоритет №1 следующей сессии.
- В рабочем дереве остаются чужие незакоммиченные правки (`CLAUDE.md`, `SESSIONS-ARCHIVE.md`, `AUDITS-ARCHIVE.md` в корне, `Standalone/price-lookup/*`) — параллельная сессия, эта сессия их не трогала.

**Что осталось:**
1. Визуальная приёмка Б-1..Б-4 (детали — в «Следующий шаг» вверху)
2. Визуальная проверка сессии 49 (batch B + зелёные)
3. План роста сессии 50 пункты 2-6 (генератор заголовков → отправка в TG → мультиформат → батч/QR → шрифт)
4. UX-15, старые жёлтые с.46, Ж-11 (живой тест генерации)
5. Этап 2: вырезать коллаж/сторис/TG-пост из основного `index.html`

### 2026-07-07 — Полный аудит инструмента + план прокачки текста/картинок (сессия 50, без правок кода)

**Контекст:** Денис попросил провести аудит content-creator моделью Fable — «как прокачать инструмент, особенно тексты/заголовки/картинки». Явно попросил не исправлять, а сначала разобраться и предложить план.

**Что сделано:** сплошное чтение всех 1749 строк `content-creator.html`, сверка спорных мест с бэкендом (`backend/server.js` — `/igdb/img`, `/pl/covers`, `/api/claude`) и с актуальной справкой по Claude API (модели, дефолты). Правок кода НЕ было.

**Найдено — причина жалобы «заголовки как рандомные слова» (не баг ИИ, три конкретные причины):** (1) названия игр идут в пост/подпись/картинку сырыми из PS Store-снапшота (`®`/`™`/коды типа `CUSA07378`/приписки платформы) — функции очистки `_cleanConsoleTitle`/`_cleanEditionTitle` в файле уже есть, но используются только для поиска в IGDB, не для отображения; (2) все 3 промпта (`generateAIPost`, `_stGenShortDescs`, `_stGenCaption`) шлют модели только список названий без контекста — на малоизвестных играх модель угадывает содержание по словам названия; (3) промпт сторис жёстко просит «максимум ~12 слов», из-за чего получаются рубленые перечисления вместо цельных фраз.

**Новые баги (Б-1..Б-4, детали и номера строк — в разделе «📝 Аудит текстового конвейера и план роста» выше):** Б-1 — потеря описаний из-за несовпадения ключей JSON (модель возвращает по названию, код ищет точным совпадением) во всех 3 ИИ-функциях; Б-2 — тихий провал парсинга JSON в `generateAIPost` (только `console.warn`, пользователь не видит потерю); Б-3 — хвост Ж-9 (апостроф всё ещё может ломать `onclick`, HTML-сущности декодируются до исполнения JS); Б-4 — `openStory` грузит обложки+HD для 5 игр, хотя рендерит максимум 3 (2 лишних похода в IGDB).

**Закрыт вопрос:** `descModel='claude-sonnet-4-6'` (Ж-11) — подтверждён валидным активным ID (Sonnet 4.6), менять не требуется. Живой тест генерации описаний всё же не проведён.

**Предложен план роста** (полностью — в разделе выше): (1) фикс текстового конвейера — ключи-индексы, контекст из `g.description`, чистые названия для отображения, few-shot в промпте сторис; (2) генератор заголовка поста + редактируемый вывод со счётчиком символов; (3) прямая отправка поста в TG-канал; (4) мультиформат экспорта (квадрат/превью-ссылка); (5) батч-режим сторис, авто-контраст текста, QR-код `?game=X`; (6) кириллический self-host шрифт для заголовков; (7) UX-15 (горячие клавиши).

**Что решили:** ничего окончательно — Денис не успел выбрать стартовую точку, диалог оборвался на прямом вопросе «с чего начинаем».

**Открытые вопросы:** порядок реализации плана роста не выбран.

**Что осталось:**
1. Денис выбирает приоритет: фикс текстового конвейера (Б-1..Б-4 + причины «рандомных заголовков») / генератор заголовков / отправка в TG / старые жёлтые баги
2. Прежние хвосты сессии 49 не сдвинулись — визуальная проверка batch B + зелёных UX-13/14/16-19, UX-15, старые жёлтые (Неон/Горизонт/тост/display:none)

_(Сессии 49 и старше перенесены в `SESSIONS-ARCHIVE.md` — команда `/session-archive`.)_


